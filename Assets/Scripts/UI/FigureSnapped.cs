using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using VRTK.Examples;

public class FigureSnapped : MonoBehaviour {

	////holoFigure is used by the player to drag to a location while this object remains snapped to the grid
	//public GameObject holoFigurePrefab;
	//private GameObject holoFigure;

	//// Use this for initialization
	//void Start () {
	//	holoFigure = (GameObject)Instantiate (holoFigurePrefab, transform.localPosition, transform.rotation);
	//	holoFigure.transform.parent = this.transform.parent;
	//	//holoFigure.transform.localScale = new Vector3 (0.02f, 0.02f, 0.02f);
	//	holoFigure.GetComponent<PlayerFigure> ().snappedFigure = this.gameObject;
	//}
	
	//// Update is called once per frame
	//void Update () {
		
	//}
}
